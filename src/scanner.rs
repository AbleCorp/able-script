mod tokens;
pub struct Scanner {
    pub start: u64,
    pub current: u64,
    pub line: u64,
}

pub fn scan() {
    let tokens: Vec<tokens::Token> = vec![];

    scan_token();

    //
}
fn scan_token() {}
